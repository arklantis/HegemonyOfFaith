Hegemony of Faith - development support folder

Purpose:
- keep local helper scripts used by QA and release checks
- store generated reports (for example i18n key extraction outputs)
- keep non-runtime notes/tools that must not affect gameplay logic

Runtime reminder:
- files in this directory are for development workflow only
- game runtime must not depend on `misc/*`
